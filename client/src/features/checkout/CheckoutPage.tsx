import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return(
        <Typography
            variant="h3"
            >
                Only Logged in user can be view this page!
            </Typography>
    )
}